// No Story

// No Description

// Only by Thinking and Testing

// Look at result of testcase, guess the code!


function testit(s){
  return s.replace(/\w{2}/g, el=>String.fromCharCode((el.charCodeAt(0) + el.charCodeAt(1)) / 2));
}
